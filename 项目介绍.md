# Word 论文自动排版项目

## 简介

从模版 docx + 文本资料 docx → 一键生成符合格式的排版 docx。小白只需放两个文件、跑一个命令，自动扫描识别。

核心思路：**格式与内容分离**。模版提供字体/字号/行距/边距等格式参数，文本资料提供章节/段落/图片/参考文献等内容。流水线自动提取两者，生成可直接运行的 python-docx 构建脚本，最终输出排好版的 docx。

## 项目结构

```
word_chat/
├── run_pipeline.py              ← ★ 一键入口
├── 从零开始配置指南.md           ← 环境安装（Python / VSCode / Claude Code）
├── 项目介绍.md                   ← 本文档
├── Templates/                   ← 放模版 docx
├── Inputs/                      ← 放文本资料 docx
├── Outputs/                     ← 格式提取 MD/JSON + 内容提取 MD/JSON
├── Paper_Project/
│   ├── Program/
│   │   ├── pipeline/
│   │   │   ├── format_extractor.py   ← Phase 1: 模版 → 格式 JSON + MD
│   │   │   ├── content_parser.py     ← Phase 2: 内容 → 结构化 JSON + 图片
│   │   │   └── script_generator.py   ← Phase 3: JSON → build_generated.py
│   │   ├── build_generated.py        ← [自动生成] 构建脚本
│   │   ├── build_acta_manuscript.py  ← 参考：Acta Materialia 期刊格式
│   │   ├── build_comprehensive_doc.py ← 参考：全功能演示（三线表/交叉引用/页码/图片）
│   │   └── master.py                 ← 参考：管线编排器骨架
│   ├── Outputs/                    ← 每次运行生成独立子目录，包含最终 docx
│   ├── Results/fig/              ← 图片目录
│   └── 基础操作.md               ← python-docx 基础操作速查
```

## 流水线四阶段

```
Templates/模版.docx ──→ [Phase 1] format_extractor.py ──→ Outputs/format.json
                                                           Outputs/格式提取.md (人工核对)

Inputs/内容.docx ──→ [Phase 2] content_parser.py ──→ Outputs/content.json
                    (图片提取到 Inputs/xxx/figures/)    Outputs/内容提取.md

format.json ──┬──→ [Phase 3] script_generator.py ──→ Program/build_generated.py
content.json ─┘                                       (所有格式值来自传参，零硬编码)

build_generated.py ──→ [Phase 4] python 运行 ──→ 最终论文.docx
```

每个阶段有内置双验证：提取器独立运行两次，比对段落数/表格数/run 数，不一致时第三轮仲裁。用户只看到 `[OK] verified (consistent)`。

## 格式提取（Phase 1）

### 双重来源，文字说明优先

1. **文字说明提取**（`_parse_text_instructions`）：解析模板中嵌入的格式说明文字
   - 如 `（一级标题小三号，加粗，居中；英文Times New Roman体）` → H1 = 15pt CENTER bold
   - 如 `（正文1.5倍行距，段落首行缩进2字符，两端对齐书写）` → body LS=1.5 indent=0.74cm
   - 自动区分 TOC 格式说明和正文格式说明（取包含"加粗"的最长匹配）
   - 中文号数精确映射：小三=15, 四号=14, 小四=12, 五号=10.5

2. **OOXML 直读**（`StyleResolver`）：直接解析 `w:rPr/w:sz`、`w:rFonts`、`w:jc` 等 XML 元素
   - 解决 python-docx API 的 `None` 问题（API 在样式继承时返回 None）
   - 通过 `styles.xml` 样式树向上追溯 base style，补齐继承值
   - 最终所有 run 的 font/size/bold/italic 全部非 None（0% None 率）

### 格式参数

提取的格式参数全部存入 `P{}` 字典，包括：
- 页面尺寸、四边距
- 正文字体/字号/行距/对齐/首行缩进
- 三级标题的字号/字体/加粗/对齐
- 页眉/页脚格式
- 封面结构（标题文字、表格标签、字体字号）
- 参考文献格式（[N] 编号、悬挂缩进）

## 内容提取（Phase 2）

### 标题层级检测

- 直接读 OOXML `w:rPr` 获取字号和粗体（绕过 python-docx API 的 None 问题）
- 排除封面日期/格式备注/公式/作者单位等非标题模式
- 支持长段落标题识别（如 `Abstract: body text` → heading="Abstract:" + body 分离）

### 图片提取

- 从段落 relationships 中提取内嵌图片，保存到 `Inputs/xxx/figures/`
- 使用纯 ASCII 安全文件名

## 脚本生成（Phase 3）

### 核心原则：零硬编码，全部传参

每个格式值要么来自 `format.json`（`P[...]`），要么是衍生计算（`D[...]`）：

| 参数 | 来源 |
|------|------|
| 页面尺寸/边距 | `P['page_w/h/mt/mb/ml/mr']` |
| 正文字体/字号 | `P['body_font/size']` (文字说明优先) |
| 行距 | `P['body_ls']` (文字说明优先) |
| 首行缩进 | `P['body_indent']` (文字说明优先) |
| 三级标题 | `P['h_levels']` (文字说明优先，OOXML 补充) |
| 页眉 | `P['header']` (OOXML 提取，剔除格式备注) |
| 封面文字/标签/字体 | `P['_text_rules']['cover']` (表格提取) |
| 封面空行数 | 从模版 body 逐元素计数 |
| 页脚字号 | `D['footer_size']` = body_size × 0.85 |
| 参考文献字号 | `D['ref_size']` = body_size - 2 |
| 图片宽度 | `D['img_width']` = min(文本宽 × 0.55, 4.2) |
| 表格单元格字号 | `D['table_cell_size']` = body_size - 3 |

### 分页算法

```
text_w_pt   = (page_w - ml - mr) / 2.54 × 72
avg_char_w  = body_size × font_ratio  (TNR=0.42, 宋体=1.0)
cpl         = text_w_pt / avg_char_w           ← 精确每行字符数
line_h      = body_size × body_ls              ← 精确行高
usable_cm   = page_h - mt - mb                 ← 精确理论可用高度，无 fudge

_ph(): 段落高度 = round(chars/cpl) × line_h     ← 四舍五入，不累积 ceil 误差
_th(): 表格高度 = body_size×2.5 + (n-1)×body_size×1.8
image: 从 wp:extent 读 EMU → pt               ← 精确图片高度
```

### 生成的脚本包含

- 封面（标签/字体/空行全从模版提取）
- 标题 + 摘要 + 关键词
- 正文（含层级标题、图片 + Fig 图注）
- 三线表（OOXML 直写，顶粗线/表头细线/底粗线）
- 交叉引用（w:anchor 书签，正文 [N] 蓝色上标 → 参考文献跳转）
- 参考文献（Acta [N] 格式，悬挂缩进，自动去重 [N] 前缀）
- 页眉（从模版提取文字和格式，剔除中文备注）
- 页脚（PAGE 域代码，动态页码）
- A4 自动分页

## 修复过的 Bug 清单

| # | 现象 | 根因 | 修复 |
|---|------|------|------|
| 1 | python-docx API 返回 None font/size | 样式继承值未 resolve | 写 `StyleResolver`，走 styles.xml 链条补齐 |
| 2 | 封面标签字体被 Table0 两个 cell 覆盖 | 遍历了所有 cell，col 1 覆盖 col 0 | 只取第一列 cell |
| 3 | "学    号：" 被关键词 "号" 误杀 | 过滤词太宽 | 改为 `['行高','居中','1.5','楷体四']` |
| 4 | 标题字号检测：36pt 封面字被当作 H1 | 未过滤低频 outlier | 字号需出现 ≥3 次才列为标题候选 |
| 5 | "小三号" 被 "三号" 先匹配成 16pt | SIZE_MAP 字典迭代无序 | 改用有序 `SIZE_PATTERNS` 列表，"小三号" 排前 |
| 6 | H2 字号被 TOC 的 "小四号" 污染 | 取了所有匹配的合并文本 | 只取含"加粗"的最长匹配（正文说明） |
| 7 | Body 行距被 TOC 备注的 "1.5倍行距" 污染 | 取了最长的正文匹配 | 优先选含 "行距"+"缩进/对齐" 的匹配 |
| 8 | Abstract 段落 >200 字符被跳过 | `detect_heading_level` 硬限制 | 支持长段落首句匹配 `Abstract:` 等标签 |
| 9 | Key words 被中文标点过滤杀掉 | `；` 在过滤列表 | 只保留 `。` 结尾判断 |
| 10 | 参考文献双序号 `[1] [1] Blackm` | 原文自带 [N] + 脚本又加 [N] | `re.sub(r'^\[\d+\]\s*', '', ref_text)` |
| 11 | 页眉含 "（新罗马字体，五号加粗居中）" | 格式备注作为文本保留 | `re.sub` 剔除中文括号备注 |
| 12 | 图片超出页面 | 宽度占文本宽 65% | 改为 55%，上限 4.2 英寸 |
| 13 | 分页提前断开，大片空白 | `ceil(chars/cpl)` 每段向上取整，多段累积虚高 | 改为 `round(chars/cpl)` 四舍五入 |
| 14 | 分页 cpl 硬编码 75 | 未用字体度量 | 字体度量表（TNR 0.42 / CJK 1.0）× body_size |
| 15 | WPS 显示明朝体 | 只设 `w:ascii`，未设 `w:eastAsia` | 中文 run 加 `rf.set(qn("w:eastAsia"), font_name)` |
| 16 | 封面空行数硬编码 6/4/2 | 未从模版读取 | 读模版 docx body 元素逐段计数 |
| 17 | 格式提取信息遗漏 | 只跑一次 | 双次独立提取 + 交叉验证 + 三轮仲裁 |

## 可能存在的问题

1. **字符宽度估算**：`font_ratio` 是标准度量值，但实际渲染受系统字体、WPS/Word 版本影响。极端情况下分页位置可能偏差 1-2 行。

2. **混合中英文段落**：中英混排段落的每行字符数难以精确。当前按 body_font 查表，如果段落内两种文字占比相近，估算会偏。

3. **封面内容填写**：封面信息表（年级专业、姓名、学号、指导教师）右侧单元格为空，需用户手动填写。内容解析器暂不提取这些字段。

4. **公式和特殊符号**：`_ph()` 对包含数学符号、上下标的段落可能低估行数。

5. **字体回退**：如果系统未安装楷体_GB2312 或 宋体，WPS/Word 会退回默认字体。`w:eastAsia` 已设置但无法强制嵌入字体文件。

6. **Office Viewer 预览**：VSCode 的 Office Viewer 插件不渲染页眉页脚，页码在插件中不可见，需用 Word/WPS 打开查看。

7. **复杂表格**：合并单元格、嵌套表格的高度估算为近似值。

8. **图片路径**：内容 docx 中的图片提取到 `Inputs/xxx/figures/`，如果文件名冲突会覆盖。

## 操作指南

1. 把模版 docx 放入 `Templates/`
2. 把文本资料 docx 放入 `Inputs/`
3. 运行 `python run_pipeline.py`（交互模式）或 `python run_pipeline.py --template X --content Y`（参数模式）
4. 查看 `Outputs/<日期_文件名>/格式提取.md` 和 `内容提取.md` 核对
5. 打开 `Outputs/<日期_文件名>/build_generated.py` 与 Claude 对话微调
6. 最终 docx 在 `Outputs/<日期_文件名>/最终论文.docx`

## 参考

- `基础操作.md`：python-docx 基础操作速查（三线表、交叉引用、页码、图片居中）
- `build_acta_manuscript.py`：Acta Materialia 期刊格式参考
- `build_comprehensive_doc.py`：全功能演示脚本
