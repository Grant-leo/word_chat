"""Submodules used by script_generator.py.

The public entry point remains ``script_generator.generate``.  These modules
hold generator-side planning rules, template rules, style-profile inference,
and reusable runtime-template fragments so the top-level generator can
gradually move away from being one large mixed-concern file.
"""
